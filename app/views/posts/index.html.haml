- @posts.each do |post|
  %li
    %div.row
      %div.col-md-4
        %img.pull-left{:src => asset_path("image.png"), :alt=> 'Imagen de la noticia'}

      %div.col-md-8
        %h3
          %a.clickable-link{id:post.id}= post.title


        %ul.footer-info
          %li 
            Fecha
            = post.created_at.strftime('%d %b %Y %I:%M %p')
          %li
            Usuario
            - user = User.find(post.userId) 
            = user.name
          %li
            Categoria
            = post.category

        %p= post.description

        %a{:href=>post.link} Seguir Link

      %ul.social-info
        %li
          %span{:id => 'li-num-votes-'+post.id.to_s}
            =post.votes
          %i.fa.fa-thumbs-o-up.fa-lg Me gusta
        %li
          %span{:id => 'li-num-comments-'+post.id.to_s}
            =post.numComments
          %i.fa.fa-comments-o.fa-lg Comentarios 
